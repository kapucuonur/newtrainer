PORT=8788
HOST=127.0.0.1
JWT_SECRET=change-me-to-a-long-random-string
# Comma-separated origins (defaults cover prod + local Vite)
# CORS_ORIGINS=https://newtrainer.trihonor.com,https://newtrainer.vercel.app,http://localhost:5173
# Cookie Secure flag (set true behind HTTPS tunnel in production)
COOKIE_SECURE=false
# SQLite + uploads live under ./data (gitignored)
# DATA_DIR=./data
