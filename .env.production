# Baked into `vite build` / `npm run build:android` (Vite production mode).
# Public Pi API — not a secret. Override locally with `.env.production.local` if needed.
VITE_API_URL=https://newtrainer-api.trihonor.com
